# Gradle
.gradle/
build/
**/build/
!gradle/wrapper/gradle-wrapper.jar

# Android / IDE
*.iml
.idea/
local.properties
captures/
.cxx/

# Release signing — never commit keystores or their passwords. Signing material
# comes from CI secrets or a local (untracked) keystore.properties.
*.jks
*.keystore
keystore.properties

# OS
.DS_Store

# Logs
*.log

# graphify generated knowledge graph (build locally with `graphify update .`)
graphify-out/
