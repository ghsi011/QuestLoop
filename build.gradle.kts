// Intentionally minimal. Each module applies the plugins it needs so that the
// pure-JVM :core module can be built and tested without resolving the Android
// Gradle Plugin (which is only required by the :app module).
